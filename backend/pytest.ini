[pytest]
minversion = 6.0
addopts = --cov=app --cov-report=term-missing
testpaths = tests
markers =
    unit: marca testes unitários isolados (com mocks)
    integration: marca testes de integração que podem ser rodados separadamente